---
name: mcp-server-setup
source_url: 'https://braze-inc.github.io/braze-docs/_user_guide/brazeai/mcp_server/setup'
indexed_at: '2026-04-05'
keywords:
  - mcp
  - server
  - setup
  - configuration
  - authentication
  - installation
  - integration
  - prerequisites
triggers:
  - how to set up mcp server
  - configure mcp
  - install mcp server
  - mcp authentication setup
  - mcp integration
---
`★ Insight ─────────────────────────────────────`
The source content is a Jekyll/Liquid template include tag (`{% multi_lang_include ... %}`), meaning the actual documentation was never rendered before being passed here. This is a common artifact of scraping docs sites that use Jekyll's `include` directives — the raw template syntax is captured instead of the resolved HTML.
`─────────────────────────────────────────────────`

## MCP Server Setup

> **Note:** Source content was a Jekyll template include (`{% multi_lang_include mcp_server/setup.md section="user" %}`) that could not be resolved. The rendered content was not available at processing time.

This topic covers user-facing setup instructions for the MCP (Model Context Protocol) server integration.

### What to Expect Here

When the source renders correctly, this section typically covers:

- Prerequisites and system requirements
- Authentication / API key configuration
- Server installation or connection steps
- Verification that the MCP server is reachable
- Common setup errors and resolutions

### Action Required

To populate this topic, either:

1. **Re-fetch** the source with a browser-rendered crawler (the Jekyll site must be rendered before scraping)
2. **Manually provide** the rendered HTML or Markdown from the `/mcp_server/setup.md` partial
